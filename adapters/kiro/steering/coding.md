# 代码生成铁律（Kiro steering）

1. **编码 UTF-8**：新建/改写源文件与文本配置一律 UTF-8。
2. **无 BOM**：UTF-8 without BOM（禁 BOM）。
3. **注释无表情**：注释与文档字符串禁止 emoji/表情符号；用 OK/FAIL/TODO 等文字。
4. `*.ps1` / `*.sh`：纯 ASCII、无 BOM；中文只写在 `.md`。
