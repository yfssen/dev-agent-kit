# 核验报告（第三轮）

> 对第二轮《修复核验报告》所列 4 项待办的落实核验，以及本轮新发现问题的修复。

---

## 一、第二轮 4 项待办：全部落实

| # | 待办 | 结果 | 证据 |
|---|------|------|------|
| 1 | `git init` | **已做**（但未提交） | `.git/` 存在，`git rev-parse` 成功；`git rev-list --count HEAD` = 0 |
| 2 | 清掉 `fixtures/fake-project/scripts/` 过期副本 | **已做** | 目录已不存在；`fixtures/README.md` 新增告警「不要在本目录维护 `scripts/` 副本」 |
| 3 | 补 dry-run 的 bash 探测 | **已做，且超出预期** | 新增 `Get-Command bash` 兜底（排除 `System32\bash.exe` WSL 启动器）；新增 `bash -n` 语法断言；新增 `\g` 拦截断言；未找到 bash 时打印 `[WARN]`，并支持 `-RequireBash` 在 CI 中强制 FAIL |
| 4 | 文档跟上新脚本 | **已做** | README 已列 `apply-mysql-dsn.ps1/.sh`（:81 / :154）与 `docs/数据库连接与复盘.md`（:151）；不再出现"四类/4 个脚本"；`操作文档.md` 的 `ADAPT-DELETE` 段已清除，步骤 3 改名「适配脚本」 |

**附带完成**：`\g` / `\G` 拦截已加入两个脚本（实测均 exit 1），第二轮标记的"残留安全边界"已闭合。

### 全量回归

| 检查 | 结果 |
|------|------|
| `.sh` 语法（`bash -n`） | 10/10 通过 |
| `.ps1` / `.sh` 非 ASCII | 0 处 |
| BOM | 0 处 |
| `phpstudy` / `ADAPT-DELETE` 残留 | 仅 `dry-run.ps1` 内的**断言字符串**（正确用法） |
| `adapters/cursor/*` ↔ `templates/cursor-rules/*` | 同步 |

---

## 二、本轮新发现并修复：结尾分号被误伤（假阳性）

### 问题

`\g` 拦截引入后，判定条件是「含 `;` 即拒」，导致**最自然的 SQL 写法全部被拒**：

| 输入 | 修复前 |
|------|--------|
| `SELECT 1;` | exit 1（被拒） |
| `SELECT * FROM t WHERE a=1;` | exit 1（被拒） |
| `SHOW TABLES;` | exit 1（被拒） |
| `DESC users;` | exit 1（被拒） |

人和 AI 写 SQL 时习惯性带结尾分号，这会造成大量无谓的"写操作被拦截"误解。

### 修法（两侧语义一致）

**先剥掉一个结尾 `;` 及其前后空白，再判是否残留 `;`。**

- `db.ps1`
  ```powershell
  $norm = $Sql.TrimEnd()
  if ($norm.EndsWith(";")) { $norm = $norm.Substring(0, $norm.Length - 1).TrimEnd() }
  if ($norm -match ';' -or $norm -imatch '\\[gG]') { ... exit 1 }
  ```
- `db.sh`
  ```sh
  NORM=$(printf '%s' "$SQL" | sed -e 's/[[:space:]]*$//' -e 's/;$//' -e 's/[[:space:]]*$//')
  # 再判残留 ';' 与 \g / \G
  ```

### 实测（两侧结果一致）

放行：

```
SELECT 1        SELECT 1;       SELECT 1 ;
SELECT * FROM t WHERE a=1;      SHOW TABLES;     DESC users;
```

拦截：

```
UPDATE x SET y=1                SELECT 1; DROP TABLE users
SELECT 1; DROP TABLE users;     SELECT 1\gDROP TABLE users
SELECT 1\G                      SELECT 1 INTO OUTFILE '/tmp/x'
DROP TABLE users;
```

关键验证点：`SELECT 1; DROP TABLE users;` **仍被拦截** —— 剥掉结尾分号后内部 `;` 依旧存在，安全性未被削弱。

---

## 三、仍待处理（3 项小项）

### 1. git 已初始化但零提交

`git rev-list --count HEAD` = 0，13 项未跟踪。
`.gitignore` / `.gitattributes` **只对已跟踪文件生效**，当前等于没生效。
建议立即首次提交 —— `.env`、`*.kit-new`、`.workbuddy-ai/`、`fixtures/fake-project/scripts/` 均已在 ignore 内，不会误提交密钥。

### 2. adapter 工具层未收录新脚本

`apply-mysql-dsn` 已进 SKILL / init-workflow / AGENTS.md / README / 操作文档 / 各工具使用指南，
但**没有进 adapter 工具层**：

```
adapters/_common/TOOLING.md          (母本)
adapters/cursor/tooling.mdc
adapters/workbuddy/rules/tooling.md
adapters/qoder/rules/tooling.md
adapters/kiro/steering/tooling.md
templates/cursor-rules/tooling.mdc   (副本)
```

而同为初始化工具的 `scan-project` / `mysql` 都在该层。此外这 6 份也没写「`;` 分隔与 `\g` / `\G` 会被拒」——
AI 日常读的是这一层，缺了容易误判。

> 注意：`_common/TOOLING.md` 是母本，其余 5 份是精简实例，改动需手工同步 6 处。

### 3. dry-run 断言覆盖不全

- `must` 列表未含 `apply-mysql-dsn.ps1/.sh` 与 `scan-project.sh` → 这两个文件若安装漏拷不会被发现
- 「机器私货」断言只查 `db.ps1`，建议改为扫描 `scripts/` 下全部 `.ps1` / `.sh`

### 另需确认

`.gitignore` 新增了 `.workbuddy-ai/`，即项目记忆日志不再进版本库。属可接受的取舍（agent 本地状态），
但若希望跨机器保留复盘记录，需调整。

---

## 四、结论

- 第二轮 4 项待办**全部落实**，其中 dry-run 的加固超出预期（多加了 `bash -n` 与 `\g` 断言）。
- 本轮发现并修复 1 处**可用性回归**（结尾分号假阳性），两侧语义一致、安全性未削弱。
- 剩余 3 项均为小项，不阻断使用。

**当前状态：可以交付了。** 唯一建议在交付前完成的是 `git commit` —— 否则版本控制形同虚设。
