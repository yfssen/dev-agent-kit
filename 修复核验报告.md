# 修复核验报告（第二轮）

> 对第一轮《完整度评估报告》所列问题的修复结果逐条实测核验。
> 环境：Windows / Git Bash；php 7.3.4 可用；本机 3306 有 MySQL 服务但无凭据。

## 收尾状态（第三轮已处理）

| 项 | 状态 |
|----|------|
| `db.sh` 引号语法 / apply-mysql-dsn 中文 | 核验方已修；现仓保持 |
| 拦截 `\g`/`\G` | **已加**（`db.ps1`/`db.sh`）；dry-run 断言 |
| 删除 `fixtures/fake-project/scripts/` 过期副本 | **已删**；README 说明由 dry-run 从 templates 拷入 |
| dry-run bash 探测 + WARN + `-RequireBash` | **已加**；无 Git Bash 时源码断言仍跑 |
| README / 操作文档跟上新脚本 | **已更新** |
| `git init` | **已执行**（尚未首次 commit，需要时再说） |
| 母本自检 | `.\fixtures\dry-run.ps1` → **DRY-RUN OK** |

---

## 一、我在核验中修掉的 2 处问题

### 1. `templates/scripts/db.sh` 语法错误 —— 脚本完全无法执行（阻断级）

新增的多语句拦截写法引号未闭合：

```sh
case "$SQL" in
  *';*)      # 单引号未闭合，吞噬下一行
```

`bash -n` 报 `syntax error near unexpected token ';'`，任何调用都直接失败。
**已改为 `*';'*`**，`bash -n` 通过。

实测（修正后）：

| 输入 | 退出码 |
|------|--------|
| `SELECT 1`（CRLF 的 .env） | 0，参数干净：`-h 127.0.0.1 -P 3306 -u fake_user -D fake_db` |
| `UPDATE x SET y=1` | 1 |
| `SELECT 1; DROP TABLE users` | 1 |
| `SELECT 1 INTO OUTFILE '/tmp/x'` | 1 |
| `SELECT 1 /* ; */` | 1（注释里的分号也拦，fail-closed） |

### 2. `templates/scripts/apply-mysql-dsn.ps1:96` 中文导致输出乱码

原行含 `docs/数据库连接与复盘.md`，PowerShell 5.1 按 GBK 解码 UTF-8 字节，
实际输出为 **`docs/鏁版嵁搴撹繛鎺ヤ笌澶嶇洏.md`** —— 正是本 kit 踩坑表第 1 条
（「`.ps1` 中文注释报解析错 → 铁律：纯 ASCII」）所描述的故障。
**已改为纯 ASCII 提示语**，输出恢复正常。

---

## 二、已改且核验通过的项

| 项 | 核验方式 | 结果 |
|----|----------|------|
| `db.ps1` 多语句拦截 | 实测 `SELECT 1; DROP TABLE users` | exit 1 |
| `db.ps1` 传播真实退出码 | 代码审查 + 实测 | 新增 `exit $LASTEXITCODE` |
| `db.ps1` 去机器私货 | 全仓 grep `phpstudy` | 模板中已无 |
| `db.sh` 多语句拦截 | 实测（语法修复后） | exit 1 |
| `db.sh` / `mysql.sh` 剥 CR | CRLF 的 `.env` 实测 | 传参无 `\r` |
| `lint.ps1` 缺 checker 判定 | 代码审查 | `$null -eq $LASTEXITCODE` → exit 2 |
| `scan-project.ps1/.sh` 去中文 | 逐行扫描 | 0 行非 ASCII |
| `dry-run.ps1` 改用 TEMP 工作区 | 代码审查 | 不再污染仓库 |
| `dry-run.ps1` 声明 php 前置、缺则 FAIL | 代码审查 | 不再静默跳过 |
| `dry-run.ps1` 覆盖 `install.ps1` | 代码审查 | 已加入 |
| `dry-run.ps1` 机器私货断言 | 代码审查 | 已加入 |
| `dry-run.ps1` 新增 `.sh` 检查 | 代码审查 | 已加入（有盲区，见三.3） |
| `.gitignore` / `.gitattributes` | 内容审查 | 合理（`.env`、`*.kit-new`、`_scan-raw.md`；`.sh` 强制 LF） |
| **新增** `apply-mysql-dsn.sh` | 命令/DSN 两种入参 + 边界实测 | 5/5 通过，密码不落 stdout，非法输入 exit 2 |
| **新增** `apply-mysql-dsn.ps1` | DSN 入参 + 边界实测 | 通过；`.env` 合并保留其他行；UTF-8 无 BOM |
| **新增** `docs/数据库连接与复盘.md` | 内容审查 | 无密码、含工具分工与写库复盘表 |
| 新功能接入 SKILL / init-workflow / AGENTS / 各工具指南 | grep | 均已接入 |
| 全仓 `.ps1`/`.sh` 纯 ASCII、无 BOM | 逐文件扫描 | 模板与工具脚本全部合规 |
| 全仓 `.sh` 语法 | `bash -n` 全量 | 16/16 通过 |

---

## 三、仍待处理（历史快照；见文首「收尾状态」）

> 以下为第二轮核验当时的待办，第三轮已处理大部分。保留原文便于对照。

### 1. 仍不是 git 仓库 —— `.gitignore` / `.gitattributes` 目前无效

`git rev-parse --is-inside-work-tree` 失败。两个文件已写好但未 `git init`，规则不会生效。
**动作**：`git init` + 首次提交（注意 `.env` 与 `*.kit-new` 已在 ignore 中，安全）。

### 2. `fixtures/fake-project/scripts/` 有 7 个过期副本，其中 2 个是**不安全版本**

过期文件：`db.ps1` `db.sh` `lint.ps1` `mysql.ps1` `mysql.sh` `scan-project.ps1` `scan-project.sh`

- `db.ps1` / `db.sh` **不含多语句拦截**（`grep -c multi-statement` = 0）
- `db.ps1` / `lint.ps1` / `mysql.ps1` 仍带 `D:\phpstudy_pro\...` 机器路径
- `scan-project.*` 仍含中文行

`dry-run.ps1:33` 每次都用 `templates\scripts\*` 覆盖到 TEMP 工作区，因此该目录**完全冗余**；
但任何人（或 AI）直接翻看 `fixtures/fake-project/scripts/db.ps1`，看到的都是旧的不安全实现。

**建议**：删除整个 `fixtures/fake-project/scripts/` 目录（dry-run 自带脚本）。
若想保留，则需在 dry-run 启动时先清空再拷贝，避免再次漂移。

### 3. `dry-run.ps1` 的 `.sh` 检查存在盲区 —— 本轮 bug 正是从这里漏过去的

只探测两个 Git-for-Windows 路径：

```
C:\Program Files\Git\bin\bash.exe
C:\Program Files\Git\usr\bin\bash.exe
```

本机两者都不存在（bash 位于 `/usr/bin/bash`），于是**整段 `.sh` 检查被静默跳过**（仅打印 Info，不计 FAIL）。
这就是 `db.sh` 的语法错误能通过"验收"的原因。

**建议**：
1. 增加 `Get-Command bash` 兜底探测；
2. 把"未找到 bash"从 `Info` 提升为 `WARN`，或提供 `-RequireBash` 开关在 CI 中强制 FAIL。

### 4. 文档未跟上新脚本

- `README.md` 目录段未列 `apply-mysql-dsn.ps1/.sh` 与 `docs/数据库连接与复盘.md`
- `README.md:26` 仍写「四类脚本」、`:76` 仍写「适配 4 个脚本」——现为 6 类（查状态/自检/验证/对账/结构扫描/连接写入）
- `操作文档.md:122` 同样写「4 个脚本」
- `操作文档.md:129` 仍指导用户「删掉 `ADAPT-DELETE` 机器私货段」，但模板中已无该段（现已移除）

---

## 四、一处残留安全边界（低危，建议加固）

`\g` / `\G` 是 mysql 客户端的语句发送符，功能**等价于 `;`**
（官方文档：`go`, `\g` — "Send the current statement to the server to be executed."）。

两个脚本只拦 `;`，不拦 `\g`：

| 脚本 | 输入 | 退出码 | 含义 |
|------|------|--------|------|
| `db.ps1` | `SELECT 1\gDROP TABLE users` | 2 | 已过白名单，仅因本机无 mysql 客户端止步 |
| `db.sh` | `SELECT 1\gDROP TABLE users` | 0 | 参数原样传给 mysql |

本机 3306 有 MySQL 服务但无凭据，**无法端到端证实执行效果**，故列为"低危待证"。
**建议**：把 `\g` / `\G` 一并纳入拦截（与 `;` 同处判定），即可彻底关闭该边界。

---

## 五、结论

- 第一轮列出的 3 处 P0/P1 **全部真实修复**，且修得对（不是表面改动）。
- 新增的 `apply-mysql-dsn` + `数据库连接与复盘` 功能**质量不错**：密码不落 stdout、`.env` 合并保留其他行、UTF-8 无 BOM、边界返回码正确。
- 但**修复过程引入了 1 个阻断级 bug**（`db.sh` 完全无法执行）——已由我修掉。
- 剩余 4 项收尾 + 1 项加固。

**当前状态：可用，但暂不建议交付给别人。**
先做 `git init`、清掉过期靶场副本、补上 dry-run 的 bash 探测，这三件事做完，
第一轮提出的"母本自检全绿"才真正成立。
