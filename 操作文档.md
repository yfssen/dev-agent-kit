# 开发智能体启动套件 · 操作手册（SOP）

> 配套 `README.md`（方法论/参考）。本文件是**照着敲就能落地**的操作步骤。
> 环境：Windows 用 `.ps1`；Linux/macOS 用 `.sh`（步骤相同）。AI 产品：按步骤 1 选装 adapter。
> 全程约 30–60 分钟。带 `【填】`=需要你写内容，`【改】`=按技术栈改命令。

---

## 0. 新电脑（先装全局 Skill，1 分钟）

把 `dev-agent-kit` 整个目录拷到新机器后，在**套件根目录**执行：

```powershell
.\install-global.ps1
```

```bash
chmod +x install-global.sh && ./install-global.sh
```

完成后，各 AI 工具可用一句话触发 `scaffold-dev-agent`（路径写在各自的 `kit-path.txt`）。再去做下面的「拷到业务项目」。

各工具的触发方式、日常口令、双 git 说明见根目录 **`各工具使用指南.md`**。

---

## 前置检查（1 分钟）

在目标项目根目录打开终端，确认：

```powershell
# 1) 在项目根目录
Get-Location
# 2) 查库客户端可用（按栈，示例 mysql）
mysql --version        # 或 psql --version
# 3) 自检工具可用（按栈，示例 php）
php --version          # 或 node --version / python --version
# 4) 有本地能跑当前代码的环境（dev server / 本地服务），记下它的 BaseUrl
```

> 若 2/3 不在 PATH，不必现在装——脚本支持用环境变量指定路径（见步骤 4）。

---

## 步骤 1 — 拷贝套件到项目（3 分钟）

假设套件在 `<套件路径>/dev-agent-kit`，在**目标项目根目录**执行：

```powershell
$kit = "<套件路径>\dev-agent-kit"   # 【改】改成实际路径

New-Item -ItemType Directory -Force .cursor\rules, scripts, docs, .kiro\steering | Out-Null

# 核心（必装）
Copy-Item "$kit\templates\AGENTS.md"                       .\AGENTS.md
Copy-Item "$kit\templates\docs\项目进度与完成度.md"        .\docs\
Copy-Item "$kit\templates\scripts\*"                       .\scripts\

# Adapter（按你用的 AI 选装，可多选）
Copy-Item "$kit\adapters\cursor\*.mdc"                     .\.cursor\rules\      # Cursor
Copy-Item "$kit\adapters\claude\CLAUDE.md"                 .\CLAUDE.md           # Claude Code
Copy-Item "$kit\adapters\kiro\steering\*"                  .\.kiro\steering\     # Kiro
Copy-Item "$kit\adapters\workbuddy\CODEBUDDY.md"           .\CODEBUDDY.md        # WorkBuddy
New-Item -ItemType Directory -Force .codebuddy\rules, .qoder\rules | Out-Null
Copy-Item "$kit\adapters\workbuddy\rules\*"                .\.codebuddy\rules\
Copy-Item "$kit\adapters\qoder\rules\*"                    .\.qoder\rules\       # Qoder
# Codex：AGENTS.md 已够用，见 adapters/codex/README.md
# 或一次性：skills/scaffold-dev-agent/scripts/install.ps1 -Adapters "cursor,workbuddy,qoder,..."
```

Unix 示例：

```bash
kit="<套件路径>/dev-agent-kit"
mkdir -p .cursor/rules scripts docs .kiro/steering
cp "$kit/templates/AGENTS.md" AGENTS.md
cp "$kit/templates/docs/"*.md docs/
cp "$kit/templates/scripts/"* scripts/
cp "$kit/adapters/cursor/"*.mdc .cursor/rules/
cp "$kit/adapters/claude/CLAUDE.md" CLAUDE.md
cp "$kit/adapters/kiro/steering/"* .kiro/steering/
chmod +x scripts/*.sh
```

拷完确认：

```powershell
Get-ChildItem AGENTS.md, scripts\db.*, scripts\lint.*, scripts\smoke.*, scripts\api-check.*
```

---

## 步骤 2 — 填记忆文件（10–20 分钟，最重要）

> 记忆的质量决定智能体的上限。**重点写"项目独有的坑"**，不是套话。

### 2.1 `AGENTS.md`
把所有 `<...>` 占位符替换成真实内容，尤其"核心约束"。

### 2.2 Adapter 进度入口
- Cursor：`.cursor/rules/project-progress.mdc`
- Kiro：`.kiro/steering/project-progress.md`
- Claude / Codex：进度以 SSOT + `AGENTS.md` 为准（`CLAUDE.md` 已指向它们）

填快照日期、整体百分比、主链路可用点/缺口；"有效文档白名单"列出真正权威的文档。

### 2.3 `docs/项目进度与完成度.md`（SSOT）
- 填产品理解、模块完成度表、已知缺口
- 第四节错位表先留空，步骤 5 用脚本自动生成

### 2.3b `docs/项目结构与架构.md`（初始化学习产出）
安装智能体时由 AI 按 `skills/scaffold-dev-agent/init-workflow.md` **一口气填完**：
仓库布局（含同目录双 git）、技术架构、CRUD 约定、二开约束、本地启动与 MySQL。

可先跑：`./scripts/scan-project.ps1 -OutFile docs/_scan-raw.md`

### 2.4（可选）新增项目专属规范
如编码规范、模型层、API 规范——按所用 AI 的规则目录追加即可。

---

## 步骤 3 — 适配脚本（10 分钟）

打开 `scripts/` 下每个脚本（`.ps1` 与 `.sh` 都要改，若两套都保留），处理所有 `# ADAPT` 标记。

### 3.1 `db.ps1` / `db.sh`（查状态）
- `.env` 路径与键名：确认指对，键名匹配你的 `.env`
- 非 MySQL：把客户端调用换成 psql/其他
- 客户端路径：默认 PATH，或用步骤 4 的环境变量（`$env:LZ_MYSQL`）
- 只读：单语句；禁止 `;` 与 mysql 的 `\g`/`\G`

### 3.1b `apply-mysql-dsn` / `mysql`（连接与写库）
- 初始化时向用户确认数据库：有库则索要连接命令，用 `apply-mysql-dsn` 写 `.env`
- 用户回复「无库」（纯前端 / 暂无后端）：复盘文档标 **跳过**，不写假 `.env`，不阻塞其余步骤（详见 `各工具使用指南.md` §5）
- 填 `docs/数据库连接与复盘.md`（无密码；或跳过）；写库操作记入复盘表

### 3.1c `scan-project`（结构扫描）
- 初始化用；输出可写入 `docs/_scan-raw.md`

### 3.2 `lint.ps1` / `lint.sh`（自检）【改】
把 checker 换成你的栈：

| 栈 | 改 checker 调用为 | 改文件过滤 |
|----|-------------------|------------|
| Node/TS | `npx tsc --noEmit`（整目录一次跑） | `*.ts` |
| Python | `ruff check` 或 `python -m py_compile` | `*.py` |

### 3.3 `smoke.ps1` / `smoke.sh`（验证）【填】【改】
- BaseUrl 默认改成**本地能跑当前代码**的地址
- 登录夹具换成你项目的 dev 登录接口
- 状态字段换成你 API 的判定字段
- 补 Public / Authed 端点清单（先放 3–5 个核心接口）

### 3.4 `api-check.ps1` / `api-check.sh`（对账）【改】
- AppDir / ApiDir / AddonDir 改成你项目的前端、后端控制器目录
- 前端调用正则改成实际写法；后端方法匹配按语言改

---

## 步骤 4 — 设环境变量（可选，避免硬编码路径）

```powershell
$env:LZ_MYSQL = "D:\path\to\mysql.exe"     # db 用
$env:LZ_PHP   = "D:\path\to\php.exe"        # lint 用（或改成你的 checker）
```

```bash
export LZ_MYSQL="/usr/local/bin/mysql"
export LZ_PHP="/usr/bin/php"
```

---

## 步骤 5 — 逐个验证（10 分钟，全绿才算搭完）

```powershell
# 1) 查状态：应返回真实数据
./scripts/db.ps1 "SELECT 1 AS ok"          # 【改】换成你库能跑的只读语句

# 2) 只读安全：应被拦截
./scripts/db.ps1 "UPDATE x SET y=1"        # 预期：报错 + exit 1

# 3) 自检：正常文件应绿
./scripts/lint.ps1 <一个真实源文件>         # 预期：OK

# 4) 缺依赖不假阳性：把 checker 路径设错，应报缺依赖 exit 2

# 5) 验证：应打通登录 + 至少 1 个接口
./scripts/smoke.ps1                          # 预期：PASS≥2 FAIL=0

# 6) 对账：应输出真实差异清单
./scripts/api-check.ps1                       # 预期：OK/NO_METHOD/NO_CONTROLLER 统计
```

Unix 把 `.ps1` 换成 `.sh`（smoke 远程：`./scripts/smoke.sh --base-url '...'`）。

跑完把第 6 步结果填回 SSOT 第四节错位表。

---

## 步骤 6 — 治理收尾（3 分钟）

```powershell
# 1) 隔离噪音/历史目录
if (Test-Path .cursorignore) { "已存在 .cursorignore，请手工合并：docs/_archive/" }
else { Set-Content .cursorignore "docs/_archive/`n" -Encoding UTF8 }

# 2) 确认密钥只在 .env，且 .env 已被 .gitignore（如用 git）
Select-String -Path .gitignore -Pattern '\.env' -Quiet
```

---

## 母本空跑（不碰业务仓）

在 **kit 仓库根目录**：

```powershell
./fixtures/dry-run.ps1
```

会把核心 + adapters 装进 `fixtures/fake-project/`，并验收：安装齐全、db 写拦截、lint 绿/缺依赖 exit 2、api-check 错位分类、smoke 打 mock 服务。

---

## 验收清单（全打勾 = 搭完）

- [ ] AGENTS.md / adapter 入口 / SSOT 已填真实内容
- [ ] `db` 能查、写操作被拦
- [ ] `lint` 正常绿、缺依赖报 exit 2（非语法错）
- [ ] `smoke` 打通 token + ≥1 接口，FAIL=0
- [ ] `api-check` 输出真实差异，结果已填回 SSOT
- [ ] 本地环境服务当前代码（改完能立即验）
- [ ] 噪音已隔离；密钥只在 `.env`
- [ ] （母本）`./fixtures/dry-run.ps1` 全绿

---

## 踩坑对照表

| 现象 | 原因 | 解决 |
|------|------|------|
| `.ps1` 中文注释报解析错 | PowerShell 5.1 (GBK) 误解码 | **铁律：`.ps1`/`.sh` 纯 ASCII、无 BOM**；中文只放 `.md` |
| 源码编码乱码 / 带 BOM / 注释有表情 | 用了 GBK 或带 BOM 保存 / 模型爱用 emoji | **一律 UTF-8 无 BOM**；**注释禁止 emoji**（见 `AGENTS.md` / `CODING.md`） |
| `"$var:xxx"` 报无效变量引用 | `$var:` 被当成盘符 | 改成 `"${var}:xxx"` |
| 单文件 `.Count` 出错 | 对单对象取 Count | `$files = @(...)` |
| 循环里连发 HTTP 全失败 | keep-alive 复用不稳 | 用 smoke 脚本单发，别写裸循环连发 |
| 自检报"一堆语法错" | checker 不在 PATH | 依赖检测：缺则 exit 2 |
| `Set-Content` 写中文乱码 | 编码不对 | 加 `-Encoding UTF8` |
| Write 工具建 `.cursorignore` 被拒 | 受保护文件名 | 用 `Set-Content` 经终端创建 |
| Cursor / Claude 各写一套进度 | 双源漂移 | 进度只认 SSOT；adapter 只做薄入口 |

---

## 日常使用（搭完之后）

```
对账(api-check) → 查状态(db) → 写码 → 自检(lint) → 验证(smoke) → 更新 SSOT
```

每次改完模块，让 AI **更新 SSOT**——这是记忆不漂移的关键。
