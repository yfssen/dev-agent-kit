# dev-agent-kit 完整度评估报告

> 评估时间：2026-09-15  
> 评估方式：**逐条实测**（跑脚本、构造输入、验证退出码），非纸面审阅  
> 评估对象：本仓库全部文档、6 家 adapter、模板脚本、安装器与母本靶场

## 修复状态（同日工程化补强）

| 项 | 状态 |
|----|------|
| P0 多语句绕过 | **已修**：`db.ps1`/`db.sh` 拒绝任何含 `;` 的输入 |
| P1 `.env` CRLF | **已修**：`db.sh`/`mysql.sh` 读取时剥离 `\r` |
| P1 lint 未执行误报 | **已修**：`$LASTEXITCODE -eq $null` → exit 2 |
| P2 dry-run | **已修**：TEMP 工作副本（幂等）、测多语句、去 phpstudy 硬编码、可选 bash、测 install 退出码 |
| P2 母本夹带机器路径 | **已修**：模板去掉 ADAPT-DELETE 私货路径，统一 `LZ_*` / PATH |
| P2 纯 ASCII | **已修**：scan-project 不再含中文；README 注明 md 文件名可中文 |
| P2 `.gitignore` | **已补** |
| P2 git/LICENSE/CI | 未做（可选，不挡工程可用） |

原报告结论「设计完整、验证不完整」针对修复前快照；请以 `.\fixtures\dry-run.ps1` 现网结果为准。

---

## 一、总体结论

**综合完整度：约 6.5 / 10 —— 设计完整，验证不完整。**

方法论、文档体系、目录结构已经达到"可交付"水准；但本 kit 最值钱的两个承诺——
**「只读安全」与「母本自检全绿」——目前都站不住**，需要修 3 处（P0×1 + P1×2）才算真正完整。

| 维度 | 得分 | 说明 |
|------|------|------|
| 文档与设计 | 9 / 10 | 方法论成熟、踩坑表来自真实经验、三份文档分工清晰 |
| 结构完整度 | 9 / 10 | 四件套 + 6 家 adapter + 靶场齐全，无 TODO 残留 |
| 可移植性 | 5 / 10 | 母本夹带作者机器路径；`.sh` 不兼容 CRLF 的 `.env`；母本自检不可复现 |
| 安全性 | 4 / 10 | 只读白名单可被多语句绕过（核心卖点失守） |
| 工程基建 | 3 / 10 | 无 git / 无 LICENSE / 无 CI；`.sh` 侧零自动化覆盖 |

---

## 二、实测发现的问题

### P0 —— 只读安全白名单可被多语句绕过（`db.ps1` / `db.sh`）

**证据（实测退出码）**

| 输入 | 结果 | 含义 |
|------|------|------|
| `db.ps1 "UPDATE x SET y=1"` | exit **1** | 正确拦截 |
| `db.ps1 "SELECT 1; UPDATE x SET y=1"` | exit **2** | **白名单已放行**（仅因本机无 mysql 客户端才止步） |
| `db.ps1 "SELECT 1; DROP TABLE users"` | exit **2** | **白名单已放行** |
| `db.ps1 "SELECT 1; DELETE FROM users"` | exit **2** | **白名单已放行** |
| `db.sh "SELECT 1; DROP TABLE users"` | exit **9** | **已实际调用 mysql**（因 CRLF 问题在 port 参数处报错，见 P1-2） |

**根因**

```powershell
# templates/scripts/db.ps1:13
if ($trimmed -inotmatch '^(SELECT|SHOW|DESCRIBE|DESC|EXPLAIN)\b') { ... exit 1 }
```

白名单用 `^` 只校验**第一条**语句；而 `mysql -e` 支持以 `;` 分隔的多语句执行。
`SELECT 1; DROP TABLE users` 首关键字是 `SELECT`，直接通过校验，随后被原样交给 mysql 执行。

**影响**

- 直接推翻 `README.md` 验收标准「写操作被拦截（只读安全）」与 `adapters/_common/TOOLING.md` 的「安全红线」
- AI 是被鼓励"大胆用 db 脚本查状态"的；一旦某条 SQL 含 `;`，破坏性语句将以库用户权限真实执行
- 注：文件 I/O 拦截（`INTO OUTFILE` 等）是**全文匹配**，不受此问题影响，仍然有效

**修复建议**：拒绝任何含语句分隔符的输入（单语句强制），或按 `;` 切分后逐条过白名单。

---

### P1-2 —— `db.sh` / `mysql.sh` 不兼容 CRLF 的 `.env`

**证据**

```
fixtures/fake-project/.env  实际字节： H O S T P O R T = 3 3 0 6 \r \n
bash scripts/db.sh "SELECT 1; DROP TABLE users"
→ mysql: [ERROR] Unknown suffix '\r' used for variable 'port' (value '3306\r')
```

**根因**

```sh
# templates/scripts/db.sh:54-60
while IFS= read -r line || [ -n "$line" ]; do
  case "$line" in
    HOSTPORT=*) HOSTPORT=${line#HOSTPORT=} ;;   # 未剥离尾部 \r
```

`${line#KEY=}` 原样保留行尾 `\r`，`HOSTNAME / DATABASE / USERNAME / PASSWORD` 同样带 `\r`。
全仓库 `.sh` 脚本**没有任何 CR 处理**（仅 `install.sh` 有一处 `tr -d ' '` 去空格）。

**影响**：Windows 上（本 kit 的主平台）生成的 `.env` 会让全部 `.sh` 查库/连库脚本失效。
`db.ps1` 不受影响（`Get-Content` 已剥离行尾）。

**修复建议**：读取后统一 `line=${line%$'\r'}`，或在解析前 `tr -d '\r'`。

---

### P1-3 —— `lint.ps1` 把「checker 没跑起来」误报成「语法错」

**根因**

```powershell
# templates/scripts/lint.ps1:44-48
$out = & $php -l $f.FullName 2>&1
if ($LASTEXITCODE -ne 0) { $fail++; Write-Host $out -ForegroundColor Red }
```

当 `$LASTEXITCODE` 为 `$null`（命令未真正执行、或未回传退出码）时，`$null -ne 0` 判定为 **true**，
于是计入失败 → 输出 `FAIL: N file(s) with errors` 并 `exit 1`。

**这恰好违反本 kit 自己的第 5 条洞见**：「工具要'响亮地失败'，缺依赖必须报缺依赖，绝不能伪装成'一堆语法错'」。
`lint.ps1` 的依赖检测只覆盖了"找不到 checker"（exit 2），没覆盖"checker 找到了但没跑成"这第三种状态。

**现实触发场景**：`php.exe` 存在但缺 VC 运行库/DLL、被杀软拦截、路径含特殊字符。

**修复建议**：显式判定 `if ($null -eq $LASTEXITCODE) { 报"checker 未执行"; exit 2 }`。

---

### P2-4 —— 母本自检（`dry-run.ps1`）不可复现，且只覆盖一半

**实测**：在本机连续跑两次 `./fixtures/dry-run.ps1`，两次都是 `DRY-RUN FAILED: 1 check(s)`，失败点都在 lint 那一步。

- 第一次：lint 找不到 php（不在 PATH）→ exit 2 → `[FAIL] lint sample expected 0, got 2`
- 第二次（显式设 `LZ_PHP`）：→ `FAIL: 1 file(s) with errors`，exit 1（由 P1-3 缺陷放大）

**结构性缺陷**

1. dry-run 未声明「php 必须在 PATH」这一前置条件，也没兜底 —— 对比 `smoke` 的 mock server 有兜底，
   但兜底里**硬编码了作者机器路径** `D:\phpstudy_pro\Extensions\php\php7.3.4nts\php.exe`（母本夹带私货）
2. dry-run **完全没跑 `.sh` 脚本**。我手工交叉验证 `.sh` 四项全绿：
   lint OK/exit 0、缺依赖 exit 2；db 写拦截 exit 1；api-check `OK=2 NO_METHOD=1 NO_CONTROLLER=1`；smoke `PASS=3 FAIL=0` exit 0
3. dry-run **完全没测 `install.ps1` / `install.sh`**（最复杂的两个脚本，共 281 行）。我手工验证其 merge-safe 行为
   与文档 **100% 一致**：空目录 → exit 0 全 `[NEW]`；重复跑 → 全 `[SAME]`；改过文件 → exit 2 + `*.kit-new` + **原文件保留**
4. dry-run **非幂等**：会就地改写 `fixtures/fake-project/scripts/*.ps1`（剥离 ADAPT-DELETE 段），跑完仓库即变脏

---

### P2-5 —— 违反自身的「纯 ASCII」铁律

```
templates/scripts/scan-project.ps1:87  L "- Agent: fill docs/项目结构与架构.md from code + this scan"
templates/scripts/scan-project.sh:59   echo "- Agent: fill docs/项目结构与架构.md from code + this scan"
```

README 铁律写「所有 `.ps1` / `.sh` 一律纯 ASCII」，而这两行含中文。
客观说该行引用中文文件名，**实际不可避免** —— 说明铁律本身缺一条"例外说明"，规则与实现自相矛盾。
（除此之外编码纪律执行得非常好：全仓库**无 BOM**，其余脚本全部纯 ASCII。）

---

### P2-6 —— 工程基建缺失

- **不是 git 仓库**：无版本控制、无 `.gitignore`、无 `LICENSE`、无 `CHANGELOG`
- **无 CI、无统一测试入口**：唯一测试是 PowerShell 版 dry-run，且如上所述覆盖面不足
- **母本同步无校验**：`adapters/_common/*` 是"共享母本"，但没有任何脚本校验它与各
  `adapters/*/rules/*` 是否同步（仅 cursor 有 `templates/cursor-rules/` 副本）→ 长期漂移风险

---

## 三、已核验正常的部分（加分项）

- **adapter 体系完整**：6 家（cursor / claude / codex / kiro / workbuddy / qoder）+ `_common` 齐全，
  每个都正确指向 `AGENTS.md`、SSOT、四类脚本；每家的"进度入口"文件都存在
- **双平台脚本对齐**：`install.ps1` 与 `install.sh` 的参数、默认值（`cursor,claude,kiro`）、
  6 个 adapter 分支（含 `workbuddy|codebuddy` 别名）完全一致
- **merge-safe 安装器行为正确**：实测三种场景（全新建 / 重复 / 有本地改动）与文档描述完全吻合，
  且不会覆盖项目已有内容 —— 这是最容易写错、也最容易坑用户的一环，做得扎实
- **`.sh` 侧四项验收全绿**（手工交叉验证，见 P2-4）
- **编码纪律**：全仓库无 BOM；除 P2-5 那 2 行外全部纯 ASCII
- **Cursor 双份副本同步**：`templates/cursor-rules/*` 与 `adapters/cursor/*` 三个文件逐一比对完全一致
- **文档体系自洽**：README（方法论）/ 操作文档（SOP）/ 各工具使用指南（触发方式）三份分工清晰、
  交叉引用正确，且「踩坑对照表」条目真实可复现（我实测踩中了其中 2 条）
- **安全设计方向正确**：白名单 + 文件 I/O 双重拦截 + 密码经 `MYSQL_PWD` 环境变量传递不进命令行 —— 思路是对的，只是白名单实现有洞

---

## 四、修复优先级建议

| 优先级 | 事项 | 工作量 |
|--------|------|--------|
| P0 | `db.ps1` / `db.sh` 拒绝多语句（堵只读绕过） | 小 |
| P1 | `.sh` 脚本剥离 `.env` 的 `\r`（兼容 CRLF） | 小 |
| P1 | `lint.ps1` 区分「checker 未执行」并报 exit 2 | 小 |
| P2 | dry-run 声明前置条件 + 去机器私货路径 + 覆盖 `.sh` 与安装器 + 幂等（用临时目录） | 中 |
| P2 | 补 `.gitignore` / `LICENSE`，纳入 git 管理 | 小 |
| P2 | 为「纯 ASCII」铁律补例外说明，或改写 scan-project 那两行 | 小 |

---

## 五、一句话定位

作为**方法论与模板资产**，这个 kit 已经成熟可用，踩坑表和四件套是真实经验的沉淀；
但作为**「拷给别人就能用」的可交付套件**，它的旗舰承诺（只读安全 + 母本自检全绿）目前经不起实测，
需要补上 P0/P1 三处修复，才能从「设计完整」走到「验证完整」。
