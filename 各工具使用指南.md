# 各 AI 工具使用指南

> 配套 `README.md`（方法论）与 `操作文档.md`（装进业务项目的 SOP）。  
> 本文只回答：**在 Cursor / Kiro / WorkBuddy / Qoder / Codex / Claude 里怎么用本套件。**

---

## 0. 一次性准备（每台电脑一次）

1. 把整个 `dev-agent-kit` 拷到本机任意目录（例如 `E:\www\1-mypro\dev-agent-kit`）。
2. 在**套件根目录**执行：

```powershell
.\install-global.ps1
```

```bash
chmod +x install-global.sh && ./install-global.sh
```

3. 脚本会把 `scaffold-dev-agent` 写入各工具全局 skills，并生成 `kit-path.txt`（指向本机 kit，**以后不必再说路径**）。
4. **改完 kit 里的 Skill / init-workflow 后**，再跑一遍 `install-global`，各工具才能用到新版（例如「必须向你要 MySQL 连接」这类规则）。

### 要不要重新安装？分两层

| 场景 | 要做什么 |
|------|----------|
| 本机刚改过 kit / 换电脑 / 不确定全局 Skill 是否最新 | 跑一次 `.\install-global.ps1`（只更新各 AI 全局 skills，**不是**重装业务项目） |
| 以后打开某个业务项目做初始化 | **不用**再跑 install-global；直接说通用口令即可触发；有库会要 MySQL，**无库可回「无库」跳过** |
| 业务项目以前装过智能体，只缺库配置 | 不必整包重装；说：「按 init-workflow 向我要 MySQL 连接，写入 .env，并填数据库连接与复盘文档」 |
| 暂时只有前端、没有后端/MySQL | 初始化时回复 **「无库」**；见下文 **§5** |

确认示例（Windows）：

| 工具 | 全局 Skill 目录 |
|------|-----------------|
| Cursor | `%USERPROFILE%\.cursor\skills\scaffold-dev-agent\` |
| Kiro | `%USERPROFILE%\.kiro\skills\scaffold-dev-agent\` |
| WorkBuddy / CodeBuddy | **桌面端优先** `%USERPROFILE%\.workbuddy\skills\scaffold-dev-agent\`；另同步 `%USERPROFILE%\.codebuddy\skills\...`；项目内还有 `.codebuddy/skills/scaffold-dev-agent/` |
| Qoder | `%USERPROFILE%\.qoder\skills\scaffold-dev-agent\` |
| Codex | `%USERPROFILE%\.agents\skills\scaffold-dev-agent\` 与 `.codex\skills\...` |
| Claude Code | `%USERPROFILE%\.claude\skills\scaffold-dev-agent\` |

---

## 1. 通用口令（装业务项目时用）

打开**要二开的工作区**（前后端同目录双 git 时：打开能同时看到两边的**父目录**），对新对话说：

```text
用 scaffold-dev-agent 安装开发智能体，并按 init-workflow 做完整项目初始化分析。
```

可选补充：

```text
只装 cursor 和 workbuddy adapter。
```

```text
只装文件，先别做深度分析。
```

期望结果：

- 写入 `AGENTS.md`、`docs/`、`scripts/`、对应 adapter
- **向你索要 MySQL 连接命令/DSN**（有库时）→ 写 `.env` → 填 `docs/数据库连接与复盘.md`
- **若暂时无后端/无库**：你回 **「无库」**，AI 把复盘文档标为跳过，继续写结构/进度（见 §5）
- 已有文件先读再合并；默认继续结构/架构初始化

你可这样回复连接信息（有库时）：

```text
mysql -h127.0.0.1 -P3306 -uroot -p你的密码 -D库名
.env 写在 backend/.env
```

无库时直接回：

```text
无库
```

---

## 2. 各工具怎么触发

### Cursor

1. 已跑过 `install-global.ps1`。
2. 打开业务项目 → Agent 聊天。
3. 直接发上面的**通用口令**（会匹配全局 skill `scaffold-dev-agent`）。
4. 装完后：`.cursor/rules/*.mdc`（`alwaysApply`）+ `AGENTS.md` 自动生效；日常直接派活即可。

若未自动挂上 skill：在对话里写 `@scaffold-dev-agent` 或明确写「按 scaffold-dev-agent skill 执行」。

### Kiro

1. 确认 `%USERPROFILE%\.kiro\skills\scaffold-dev-agent\` 存在。
2. 打开业务项目 → 新开 Agent 会话（改完 skill 后建议新开）。
3. 发送通用口令。
4. 可在面板 **Agent Steering & Skills** 里确认已加载该 skill。
5. 装完后：项目内 `.kiro/steering/` + `AGENTS.md` 生效。

### WorkBuddy（CodeBuddy）

WorkBuddy 桌面端与 CodeBuddy 共用一套思路，但**技能目录有两套**，且容易误走「技能市场」：

| 位置 | 路径 | 作用 |
|------|------|------|
| 用户级（WorkBuddy） | `%USERPROFILE%\.workbuddy\skills\scaffold-dev-agent\` | 桌面端默认找这里 |
| 用户级（CodeBuddy） | `%USERPROFILE%\.codebuddy\skills\scaffold-dev-agent\` | CLI / 部分面板 |
| 项目级（更稳） | `<项目>/.codebuddy/skills/scaffold-dev-agent/` | 装 workbuddy adapter 时自动写入；本项目可被直接索引 |

#### 正常用法

1. 套件根跑过 `.\install-global.ps1`（同时写入上面两个用户级目录）。
2. 打开业务项目 → **新开**对话（旧会话常不认刚同步的 skill）。
3. 发通用口令；可用 `/skills` 或面板「专家·技能」确认有 `scaffold-dev-agent`。
4. 装完后读根目录 `CODEBUDDY.md` + `.codebuddy/rules/`；业务知识仍以 `AGENTS.md` + `docs/` 为准。
5. 改过 kit / skill / 规则后：再跑 `install-global`，并**新开会话**。

> **注意**：`init-workflow` **不是**单独技能名，它是 skill 目录里的 `init-workflow.md`。口令里不要拆成两个技能名去搜，否则会触发市场搜索。

#### 手动补项目级 skill（用户级有、项目里没有时）

在 PowerShell（把路径换成你的）：

```powershell
$kit = "E:\www\1-mypro\dev-agent-kit"
$proj = "E:\www\1-mypro\your-project"   # 换成当前业务项目根
$dest = Join-Path $proj ".codebuddy\skills\scaffold-dev-agent"
New-Item -ItemType Directory -Force -Path "$dest\scripts" | Out-Null
Copy-Item "$kit\skills\scaffold-dev-agent\SKILL.md","$kit\skills\scaffold-dev-agent\adapter-map.md","$kit\skills\scaffold-dev-agent\init-workflow.md" $dest -Force
Copy-Item "$kit\skills\scaffold-dev-agent\scripts\*" "$dest\scripts" -Force
Set-Content "$dest\kit-path.txt" -Value $kit -Encoding ascii
```

或对已有项目再跑一次 install（merge-safe），指定 workbuddy：

```powershell
& "$kit\skills\scaffold-dev-agent\scripts\install.ps1" -KitRoot $kit -ProjectRoot $proj -Adapters "workbuddy"
```

#### WorkBuddy 一直去市场搜、说「本地没有」

常见原因：会话触发了内置 `find-skills`，去 SkillHub / 推荐市场搜名字，**无视本机已安装目录**。

处理：

1. 再跑 `.\install-global.ps1`
2. 确认项目级 `SKILL.md` 存在（见上表；没有就用手动拷贝）
3. **关掉当前任务，新开对话**，改用「绕过市场」口令：

```text
不要用 find-skills，不要去 SkillHub/市场搜索。
本机技能已在：
- %USERPROFILE%\.workbuddy\skills\scaffold-dev-agent\SKILL.md
- 本项目 .codebuddy/skills/scaffold-dev-agent\SKILL.md
KIT 路径见同目录 kit-path.txt。
请直接读取上述 SKILL.md 与 init-workflow.md，对当前项目做 merge-safe 安装与完整初始化。
有库要 MySQL；无库回「无库」即可跳过。先读 AGENTS.md / CODEBUDDY.md / docs/，不要 Force 覆盖。
```

项目若已有 `AGENTS.md` / `docs/`：只是 WorkBuddy 认不出 skill 时，用上面口令做增量即可，**不必推倒重装**。

### Qoder

1. 确认 `~\.qoder\skills\scaffold-dev-agent\` 存在。
2. 打开业务项目 → Agent。
3. 发送通用口令；输入 `/` 可查看 Skills 列表（以实际 UI 为准）。
4. 装完后：`AGENTS.md` + `.qoder/rules/` 生效（与 AGENTS 冲突时 rules 优先）。

### Codex

1. 确认 `~\.agents\skills\scaffold-dev-agent\`（或 `~\.codex\skills\...`）存在。
2. 打开业务项目。
3. 发送通用口令。
4. 装完后主要靠根目录 **`AGENTS.md`**（Codex 原生）；无需额外入口文件。

### Claude Code

1. 确认 `~\.claude\skills\scaffold-dev-agent\` 存在。
2. 在项目目录启动 Claude Code。
3. 发送通用口令。
4. 装完后：根目录 `CLAUDE.md` 指向 `AGENTS.md` + SSOT。

---

## 3. 装完之后日常怎么用（各工具相同）

直接下任务，不必再提 skill，例如：

```text
按闭环做：先 api-check，再查相关表，改绑定接口，lint，smoke，更新 SSOT。
```

常用命令（在项目根，Windows 示例）：

| 用途 | 命令 |
|------|------|
| 结构扫描 | `.\scripts\scan-project.ps1` |
| MySQL 交互 | `.\scripts\mysql.ps1` |
| 连接命令写入 .env | `.\scripts\apply-mysql-dsn.ps1 -Command 'mysql -h... -u... -p... -D...'` |
| 只读查库 | `.\scripts\db.ps1 "SELECT 1"` |
| 自检 | `.\scripts\lint.ps1 <文件或目录>` |
| 冒烟 | `.\scripts\smoke.ps1` |
| 对账 | `.\scripts\api-check.ps1` |

Unix 把 `.ps1` 换成 `.sh`。

闭环：

```text
api-check → db → 写码 → lint → smoke → 更新 docs/项目进度与完成度.md
```

生成代码铁律（已写进规则）：**UTF-8、无 BOM、注释无表情符号**。

---

## 4. 双 git（前后端同文件夹）注意

```text
工作区父目录/          ← 用 AI 打开这一层；kit 文件装在这里
  backend/   (.git)
  frontend/  (.git)
```

- 改后端就在 `backend` 仓库提交；改前端同理。
- `.env` 多在后端：安装分析后应把 `scripts/db.ps1`、`mysql.ps1` 的 `# ADAPT` `.env` 路径指到后端。

---

## 5. 暂时只有前端 / 无后端无 MySQL

官网、营销站、纯 Vue/静态页等：**当前没有后端，更没有 MySQL**——这是正常阶段，不是初始化失败。

### AI 应该怎么做

1. 仍按 init-workflow **问一次** MySQL（确认你不是漏配）。
2. 你回复 **「无库」**（或「暂无后端/暂无数据库」）后：
   - **不写** `.env`，不跑 `db` / `mysql`
   - 把 `docs/数据库连接与复盘.md` 标为 **跳过（纯前端 / 暂无库）**
   - 在 `AGENTS.md`、`docs/项目结构与架构.md`、SSOT 写明：**当前仅前端，后端与库待建**
   - 继续完成结构/进度文档；`api-check` / `smoke` 可标 N/A 或只对静态/前端冒烟
3. **不要**反复追问密码，也**不要**假装有库去猜连接。

### 你怎么回

```text
无库
```

或更完整：

```text
无库。当前纯前端官网，表单还是前端模拟；后端和 MySQL 以后再接。
```

### 以后接上后端时

走 **§6 先前端、后加后端**：再要 MySQL、写 `.env`、改 `# ADAPT`、把复盘文档从「跳过」改成正式连接摘要。

一句话：**无库可以合法跳过；问过 + 记过「跳过」就算完成，不是半成品。**

---

## 6. 先前端、后加后端（结构演进）

常见情况：项目一开始只有前端，页面做得差不多了，再新建 `backend/` 接入接口。  
**不用重装智能体**，也**不必 `-Force` 覆盖**已有规则；这是「结构升级」，补一轮学习 + 改几个 `# ADAPT` 即可。

若初始化时已按 §5 回过「无库」，本阶段补上连接即可。

### 推荐目录

若原先工作区根就是前端，建议收成：

```text
工作区父目录/          ← 用 AI 打开这一层；AGENTS.md、scripts/、docs/ 仍在这里
  frontend/            ← 原来的前端（可自有 .git）
  backend/             ← 新建后端（可另开 .git）
```

智能体文件继续放在**能同时看见前后端的那一层**，不要装进 `frontend/` 里。

### 通用口令（结构升级时用）

```text
项目原先只有前端，现在要新增 backend 并接入接口。
请按 init-workflow 做结构升级：
1. 再跑 scan-project
2. 更新 docs/项目结构与架构.md、AGENTS.md（改成双目录/双 git）
3. 向我要 MySQL 连接，.env 写到 backend/.env（若仍无库再说「无库」）
4. 改 scripts 里 # ADAPT（.env 路径、lint/smoke/api-check 前后端路径）
5. 更新 SSOT：前端已完成的页面、待接后端接口清单
不要 Force 重装已有规则，先读再合并。
```

### 你要盯的几件事

| 项 | 做什么 |
|----|--------|
| `.env` | 放到 `backend/.env`；`db` / `mysql` / `apply-mysql-dsn` 的 `# ADAPT` 指过去 |
| `api-check` | 配前端 API 调用目录 + 后端控制器目录，开始对账 |
| `smoke` | BaseUrl 改成真实后端地址 |
| 提交 | 改前端进 `frontend` 仓；改后端进 `backend` 仓；kit/docs 在父目录仓（若有） |

### 不必做的

- 不必整包重装 skill / 不必 `-Force` 覆盖规则
- 不必因为「初始化时没后端」就当失败——当时记「仅前端 / 无库」是对的，现在**补记后端与库**即可

一句话：**智能体跟着工作区升级，不是推倒重来；加后端 = 重扫 + 更新记忆文档 + 把工具指到后端。**

---

## 7. 同一项目换多家 AI（Cursor → Codex / Claude / WorkBuddy / Qoder …）

**一套项目记忆，多家只挂「薄入口」**——换工具不会各写一份架构，也不会默认互相覆盖。

### 怎么分工

| 层 | 文件 | 谁读 |
|----|------|------|
| **共享核心（一份）** | `AGENTS.md`、`docs/`、`scripts/`、`.env` | 所有工具都认这一份 |
| **各家薄入口** | `.cursor/rules/`、`CLAUDE.md`、`CODEBUDDY.md`、`.qoder/rules/`、`.kiro/steering/` … | 只负责「自动加载核心」，**不写业务知识** |

Codex 常常**不用额外文件**，直接读 `AGENTS.md`。  
同一项目可同时装 Cursor + Claude + WorkBuddy + Qoder 等（见 `adapters/README.md`）。

### 通用口令（已有智能体，只补别家入口）

例如 Cursor 先装过，后来要用 Codex / Claude / WorkBuddy / Qoder：

```text
本项目已有开发智能体。请只补装 codex,claude,workbuddy,qoder adapter（merge-safe），
先读 AGENTS.md 和 docs/，不要 Force 覆盖，不要重做完整初始化。
```

安装器默认 **merge-safe**：已有文件保留，冲突写成 `*.kit-new`，先读再合并。

### 日常约定

- **进度只认一份 SSOT**：换工具后第一件事让 AI 读 `docs/项目进度与完成度.md`，别让它「重新摸一遍当从零」。
- **业务坑只写进 `AGENTS.md` / docs**，别写进某家 adapter（否则只有那家看得见）。
- 各工具同一套闭环：读同一份记忆 → 跑同一套 `scripts/` → 改完更新同一份进度文档。
- 若某次会话乱改了架构文档：用 git 回看；口令里可加「以现有 docs 为准，只增量更新」。

一句话：**项目知识在仓库里共享；各 AI 只是不同遥控器，换遥控器不换大脑。**

---

## 8. 常见问题

| 现象 | 处理 |
|------|------|
| 一句话没反应 / 找不到 skill | 再跑 `.\install-global.ps1`；新开 AI 会话；在该工具 Skills 面板确认目录 |
| WorkBuddy 去市场搜、说本地没有 scaffold-dev-agent | 见 **§2 WorkBuddy**：确认双用户级目录 + 项目级 `.codebuddy/skills/`；新开对话用「绕过市场」口令；勿把 init-workflow 当第二个技能名去搜 |
| 问你 kit 路径 | 检查对应全局 skill 下是否有 `kit-path.txt`，内容是否为本机绝对路径 |
| 改完 kit 后项目初始化行为还是旧的 | 再跑 `.\install-global.ps1`，并**新开** AI 会话后再说通用口令 |
| 初始化时没问 MySQL | 全局 Skill 可能不是最新；跑 install-global 后重试；或手动说「向我要 MySQL 连接并写 .env / 复盘文档」 |
| 初始化一直逼要库，但项目纯前端 | 直接回 **「无库」**；见 **§5**；并确认已 `install-global` 同步最新 Skill |
| 覆盖了已有 AGENTS.md | 安装器默认 merge-safe：看 `*.kit-new`，先读再合并，不要用 `-Force` 除非你明确要清空 |
| 只想换电脑继续用 | 拷贝整个 kit → `install-global` → 打开业务项目说通用口令 |
| 暂时只有前端、无后端无 MySQL | 见 **§5**；回「无库」即可合法跳过 |
| 原先只有前端，现在要加 backend | 见 **§6 先前端、后加后端**；不用重装，发结构升级口令即可 |
| 同一项目换 Cursor / Codex / Claude 等 | 见 **§7 同一项目换多家 AI**；只补 adapter，共享 `AGENTS.md` + docs |

---

## 9. 文档索引

| 文件 | 用途 |
|------|------|
| 本文件 `各工具使用指南.md` | 各 AI 里怎么触发、日常怎么用 |
| `README.md` | 方法论、目录、验收 |
| `操作文档.md` | 拷进业务项目的逐步 SOP |
| `skills/scaffold-dev-agent/SKILL.md` | Skill 正文（给 AI 读） |
| `skills/scaffold-dev-agent/init-workflow.md` | 二开一口气初始化分析 |
| `adapters/README.md` | 各家配置目录对照 |
