# <项目名> AI 开发指南

> 本文件是 AI 进入项目的入口。用简体中文，突出"项目独有的坑"。
> 长文架构见 `docs/项目结构与架构.md`；进度只认 `docs/项目进度与完成度.md`。

## 项目简介

<一句话说清：什么技术栈、什么业务、基于什么二开（如有）>

| 目录 | 说明 |
|------|------|
| `<后端目录>/` | 后端：<框架/分层>；自有 git：<是/否> |
| `<前端目录>/` | 前端：<框架>；自有 git：<是/否> |
| `docs/` | 项目文档（中文） |
| `docs/项目结构与架构.md` | 结构/架构/CRUD/约束（初始化学习产出） |
| `docs/数据库连接与复盘.md` | MySQL 连接摘要（无密码）+ 写库复盘日志 |
| `docs/项目进度与完成度.md` | **进度唯一事实来源（SSOT）** |

### 仓库布局

- 类型：`<单仓 / 同目录双 git / 其他>`
- 约定：前后端分立 git 时，**改哪侧就在哪侧仓库提交**；本文件与 `scripts/` 放在工作区根（同时看到两侧的那一层）。

## 核心约束（必须遵守）

> 写"和标准框架不一样的地方""二开踩过的坑""必须的规矩"。

1. **<框架偏差>**：
2. **<返回/参数约定>**：
3. **<日志/事务约定>**：
4. **代码风格**：方法体尽量短、中文注释、简单直接、不过度封装
5. **编码 UTF-8**：源码与文本配置一律 **UTF-8**（不要 GBK/GB2312/UTF-16）
6. **无 BOM**：UTF-8 **不带 BOM**（禁止 `EF BB BF` / `U+FEFF`）
7. **注释无表情**：注释与文档字符串禁止 emoji/表情符号；用文字代替（如 OK/FAIL/TODO）
8. **语言**：对话/注释/文档用简体中文；变量/类名用英文

## 开发数据库 / 环境

| 项 | 值 |
|----|-----|
| 主机 | `<host>` |
| 端口 | `<port>` |
| 库名 | `<db>` |
| 用户 | `<user>` |
| 前缀 | `<prefix>` |
| 权限 | 开发账号通常具备增删改查（全权限以实际授权为准） |

- 配置：`<.env 路径，双 git 时多为 backend/.env；无库时写「暂无 / N/A」>`（**密码只留此处**）
- 复盘：`docs/数据库连接与复盘.md`（无密码；写库操作必须记日志；纯前端可标「跳过」）
- 初始化时 AI **必须向你确认** MySQL：有库则索要连接命令/DSN 并用 `apply-mysql-dsn` 写入 `.env`；你回复「无库」则跳过并记入复盘

## 工具入口（AI 直接用，别说"无法访问"）

| 用途 | Windows | Unix |
|------|---------|------|
| 写入/更新 .env（连接命令） | `./scripts/apply-mysql-dsn.ps1 -Command '...'` | `./scripts/apply-mysql-dsn.sh --command '...'` |
| 项目结构扫描 | `./scripts/scan-project.ps1` | `./scripts/scan-project.sh` |
| MySQL 交互 / 批准后的写库 | `./scripts/mysql.ps1` | `./scripts/mysql.sh` |
| 查真实状态（只读单语句） | `./scripts/db.ps1 "<只读SQL>"` | `./scripts/db.sh "<只读SQL>"` |
| 语法/类型自检 | `./scripts/lint.ps1 <文件或目录>` | `./scripts/lint.sh <文件或目录>` |
| 接口冒烟 | `./scripts/smoke.ps1` | `./scripts/smoke.sh` |
| 接口错位对账 | `./scripts/api-check.ps1` | `./scripts/api-check.sh` |

## 规范索引

| 规则 / 入口 | 适用 |
|-------------|------|
| `docs/项目结构与架构.md` | 结构、架构、CRUD、统一规则 |
| adapter 工具层 | 查状态/自检/验证/对账 |
| adapter 进度入口 | 进度快照 + AI 行为要求 |
| `<其他项目规范>` | <编码/模型层/API 等> |
