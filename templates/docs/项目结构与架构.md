# 项目结构与架构（学习文档）

> 安装智能体时的**标准初始化产出**。跨对话了解项目先读本文 + `AGENTS.md`。
> 进度不写这里——进度只认 `docs/项目进度与完成度.md`。

## 0. 仓库布局

| 项 | 值 |
|----|-----|
| 工作区根 | `<当前打开的根目录>` |
| 布局类型 | `<单仓 / 同目录双 git（前后端分立）/ 其他>` |
| 后端路径 | `<path>`　自有 git：`<是/否>` |
| 前端路径 | `<path>`　自有 git：`<是/否>` |
| 提交约定 | 改哪侧就在哪侧仓库提交；工作区根通常不统一 commit |

## 1. 一句话架构

<技术栈 + 部署形态 + 二开基线（如有）>

## 2. 目录地图（只列关键）

| 路径 | 职责 |
|------|------|
| | |

## 3. 后端

- 语言/框架/版本：
- 入口（public/index、main、等）：
- 分层（controller / service / model / validate…）：
- 路由/API 前缀约定：
- 鉴权方式：
- 配置与 `.env` 位置：

### 3.1 增删改查约定

| 动作 | 路由/方法命名习惯 | 备注 |
|------|-------------------|------|
| 列表 | | |
| 详情 | | |
| 创建 | | |
| 更新 | | |
| 删除 | | |

### 3.2 响应与错误

<统一 code/message/data 形态；特殊例外>

## 4. 前端

- 语言/框架/版本：
- 入口与构建：
- 状态管理 / 路由：
- 请求封装位置与写法（`url:` / axios / uni.request…）：
- 多端（H5/小程序/App）差异：

## 5. 开发约束与统一规则（二开重点）

> 写清「和标准框架不一样」的地方；这些应同步进 `AGENTS.md` 核心约束。
> 默认铁律（所有项目）：源码 **UTF-8**、**无 BOM**；**注释禁止表情符号/emoji**。

1.
2.
3.

## 6. 本地开发

| 项 | 命令或说明 |
|----|------------|
| 启动后端 | |
| 启动前端 | |
| MySQL 交互 | `./scripts/mysql.ps1` 或 `./scripts/mysql.sh` |
| 只读查库 | `./scripts/db.ps1 "SELECT …"` |

## 7. 初始化扫描摘录

> 可粘贴 `scan-project` 关键输出，或写「见 docs/_scan-raw.md」。

## 8. 未知项 / 待核实

- ❓
