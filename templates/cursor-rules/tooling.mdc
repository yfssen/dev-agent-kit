---
description: 项目智能体工具层：查状态、自检、验证、对账的固定入口
alwaysApply: true
---

# 工具层（AI 自主干活的固定入口）

本项目已具备可用工具，遇到相应任务**直接用**，不要说"无法访问"。
脚本：Windows `*.ps1` / Unix `*.sh`（逻辑相同）。完整说明见套件 `adapters/_common/TOOLING.md`。

## 0. 扫描 / 连接写入 / MySQL

```powershell
./scripts/scan-project.ps1
./scripts/apply-mysql-dsn.ps1 -Command 'mysql -h.. -u.. -p.. -D..'
./scripts/mysql.ps1
# Unix: ./scripts/scan-project.sh | ./scripts/apply-mysql-dsn.sh --command '...' | ./scripts/mysql.sh
```

双 git：`.env` 常在后端目录，改 `# ADAPT`。
`apply-mysql-dsn`：初始化时把连接命令/DSN 解析写入 `.env`（密码只留 `.env`）；无库跳过。

## 1. 查真实状态（只读）

```powershell
./scripts/db.ps1 "<只读 SQL>"        # ADAPT
# Unix: ./scripts/db.sh "<只读 SQL>"
```

- 脚本自动读 `.env`，密码经环境变量传入，**不出现在命令行**
- **安全红线**：白名单——只允许**单条**只读语句（禁止 `;` 与 mysql `\g`/`\G`）；写操作/文件 I/O 被拦截
- 客户端：`$env:LZ_MYSQL` / `$LZ_MYSQL` > PATH；找不到报 `exit 2`

## 2. 自检（改完代码必跑）

```powershell
./scripts/lint.ps1 <文件或目录>       # ADAPT
# Unix: ./scripts/lint.sh <文件或目录>
```

- **缺依赖时报 `exit 2`，绝不误报成语法错**；真错才 `exit 1`
- 规则：**任何代码编辑后，收尾前必须自检通过**
- ADAPT：先 `cd` 子目录再跑 eslint 时，相对路径先按工作区根解析；`Join-Path` 的 env base 可能为空，须先判空

## 3. 验证（接口/功能冒烟）

```powershell
./scripts/smoke.ps1
./scripts/smoke.ps1 -BaseUrl "<远程>"
# Unix: ./scripts/smoke.sh [--base-url <远程>]
```

- 用测试夹具自动取 token；`PASS` / `WARN` / `FAIL` 含义见 AGENTS.md
- **本地环境要服务当前代码**，改完即可本地验证

## 4. 对账（谈进度/改错位前必跑）

```powershell
./scripts/api-check.ps1
# Unix: ./scripts/api-check.sh
```

- 前端调用 ↔ 后端方法；**更新 SSOT 前先跑它**
- 默认只打 Summary + 前 20 条错位（省 token）；全量加 `-All` / `ALL=1`

## 5. 标准工作闭环

```
定位现行逻辑（SSOT 落点 / rg / scan 的 Git hot files）
  → 只读命中文件（不要整仓、不要整份 _scan-raw）
  → 对账(api-check 默认摘要) → 查状态(db)
  → 按规范写码 → 自检(lint 命中文件) → 验证(smoke) → 更新 SSOT 触及的行
```

**证明没猜错** = lint 绿 + smoke 通 + api-check Summary 与改动一致，而不是模型「觉得对」。
